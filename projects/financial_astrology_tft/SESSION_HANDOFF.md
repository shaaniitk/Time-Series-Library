# Session Handoff

> Update this file at the end of every session that materially changes the
> project. Keep it short; detailed evidence belongs in the tracker and scorecard.

## Latest Handoff

```text
date: 2026-08-01 14:05 Asia/Kolkata
phase: native semantic hardening / data remediation
active_task: TFT-SR02 claimed by Codex /root; FA-DATA-001 waiting external
implementation_authorized: plan and data audit yes; NIFTY neural training no
files_changed: TFT-SR00 semantic/checkpoint code; TFT-SR01 reproducibility/data/experiment code and tests; immutable legacy manifest/verifier; root/project trackers; DATA_AUDIT_REPORT.md
tests_run: SR01 combined gate 101 passed; independent audit 65 focused plus 89 broader passed; legacy verifier 65 records; py_compile and git diff hygiene passed
evidence_paths: root EV-IMP-025 and EV-IMP-026; project EV-TFT-SR00-001 and EV-TFT-SR01-001; DATA_AUDIT_REPORT.md; EV-DATA-001-BLOCKING-AUDIT
live_external_processes: none matching TFT matrix/run.py at 23:36; matrix completed 14/14 at 23:22
blockers: raw NIFTY OHLC/session keys; PySwissEph generator/convention manifest; native TFT-SR02..09 incomplete
next_safe_action: continue TFT-SR02; resume FA-DATA-001 when source/generator package arrives
```

### Completed this session

- Confirmed the TFT feature matrix exited with all 14 result/checkpoint
  directories; the final experimental profile recorded 0.04245717 MSE.
- Audited the meaning of the weak advanced-feature results and separated model
  quality from semantic implementation defects.
- Defined `TFT-SR00`–`TFT-SR09`, with legacy versioning, neutral/no-op,
  reproducibility, known-answer, mask, gradient, and release tests.
- Integrated the semantic gate into the astrology implementation plan, tracker,
  orchestrator, decisions, scorecard, and root analysis report.
- Defined the first NIFTY experiment as a frozen market/calendar base with
  paired disabled, matched-null, and real-planet residual arms.
- Reserved long astronomical memory for phase, relative phase, interval events,
  response banks, and coarse slow states rather than a decades-long LSTM.
- Audited `comprehensive_dynamic_features_nifty.csv` and
  `nifty50_returns.csv` without mutation and recorded their hashes, schemas,
  ranges, circular invariants, and alignment defects.
- Proved every supplied rashi pair uses the same off-by-one/clipping transform;
  rejected those fields, quarantined Shadbala, and reduced nodes to one axis.
- Detected mixed market-session labels, including 412 strict return-vector
  matches to the following day's bar; prohibited heuristic relabelling.
- Defined the provisional 37-column `CLASSICAL_CONTINUOUS_V1` feature slice and
  the exact source/generator package required before it is admitted.
- Completed `TFT-SR00`: separately froze the historical producer and explicit
  v1 replay scripts, verified 65 immutable records, added semantic identities,
  bound checkpoint metadata, guarded old-setting replay, and passed 158 native
  tests.
- Completed `TFT-SR01`: isolated every production RNG stream, made train-only
  sample order deterministic, added strict paired common-state construction,
  bound run/fold/pair manifests, removed epoch-level test evaluation, and
  passed independent adversarial review.

### Not completed

- `TFT-SR02` through `TFT-SR09` are not complete; `TFT-SR02` is active.
- `FA-DATA-001` is not complete: raw source OHLC and the PySwissEph generator
  were not supplied, so corrected rows cannot yet be reproduced.
- No astronomical convention has been frozen.
- No financial-astrology loader, feature, model, or experiment code has been
  written.

### Important warning for the next agent

Do not mark `FA-TFT-SEM-001` complete merely because `TFT-SR00`/`TFT-SR01` passed; root
`TFT-SR02` through `TFT-SR09` remain. Do not repair the mixed market dates with a global
offset or weekend rule, and do not use the supplied rashi/Shadbala columns in a
model. Do not launch NIFTY neural training before `TFT-SR09`, corrected data,
loader, leakage, and baseline gates. Do not open a future/locked test set. Do
not merge outer planets into a result labeled “classical Vedic.”

## Handoff Template

Copy and replace the `Latest Handoff` section:

```text
date:
phase:
active_task:
implementation_authorized:
files_changed:
tests_run:
evidence_paths:
live_external_processes:
blockers:
next_safe_action:
```
